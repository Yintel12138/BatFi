name: Build

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "**" ]

jobs:
  build:
    runs-on: macos-15
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_16.3.app

      - name: Create AnalyticsDSN stub
        run: |
          cat > BatFiKit/Sources/ClientsLive/AnalyticsDSN.swift << 'EOF'
          import Foundation

          // This file is automatically generated in the CI process.
          // Do not modify or check into version control.
          let analyticsDSN = ""
          EOF

      - name: Build BatFi
        run: |
          set -o pipefail
          xcodebuild build \
            -project BatFi.xcodeproj \
            -scheme BatFi \
            -destination "generic/platform=macOS" \
            -derivedDataPath build \
            -skipMacroValidation \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            | xcpretty || xcodebuild build \
              -project BatFi.xcodeproj \
              -scheme BatFi \
              -destination "generic/platform=macOS" \
              -derivedDataPath build \
              -skipMacroValidation \
              CODE_SIGN_IDENTITY="" \
              CODE_SIGNING_REQUIRED=NO \
              CODE_SIGNING_ALLOWED=NO

      - name: Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: BatFi-macOS-Binary
          path: build/Build/Products/Debug/*.app
          if-no-files-found: error
          retention-days: 7
